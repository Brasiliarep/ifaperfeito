
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ifaoluwo.app',
  appName: 'IfaOluwo',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
